# Analysis of ensemble means

To focus on spatial generalizability of the model, take the mean over all parameter values to predict only as a function of geometry etc.

Manuscript figures:

* 3 (`plot_hyperparameter_sensitivity.py`)
* 4 (`plot_hyperparameter_sensitivity.py`)
* 6 (`flowlines.py`)
* C1 (`future_flowlines.py`)
* D1 (`plot_trees.py`)
* D2 (`compute_shap.py`)
* D3 (`compute_shap.py`)

Tables:

* C1 (`transform_N.py` and `flowlines.py`)
* C2 (`transform_N.py`)
* C3 (`future_flowlines.py`)

